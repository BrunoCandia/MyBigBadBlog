# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: my-big-bad-blog
services:  
  app:
    language: dotnet
    project: ./MyBigBadBlog.AppHost/MyBigBadBlog.AppHost.csproj
    host: containerapp
